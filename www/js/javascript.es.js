'use strict'

document.addEventListener('DOMContentLoaded', () => {
  const userName = '鈴木'
  if (userName) {
    alert('こんにちは${userName}さん')
  }

  const address = ''
  if (address) {
    alert('あなたは${userName}に住んでいますね？')
  }
})
